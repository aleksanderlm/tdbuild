diff --git a/Telegram/SourceFiles/api/api_transcribes.cpp b/Telegram/SourceFiles/api/api_transcribes.cpp
index 7c6b271ca5..c11b9d412a 100644
--- a/Telegram/SourceFiles/api/api_transcribes.cpp
+++ b/Telegram/SourceFiles/api/api_transcribes.cpp
@@ -12,6 +12,7 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "data/data_channel.h"
 #include "data/data_document.h"
 #include "data/data_peer.h"
+#include "data/data_file_origin.h"
 #include "data/data_session.h"
 #include "history/history.h"
 #include "history/history_item.h"
@@ -22,6 +23,14 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "main/main_session_settings.h"
 #include "spellcheck/spellcheck_types.h"
 
+#include <QtCore/QCoreApplication>
+#include <QtCore/QDir>
+#include <QtCore/QTimer>
+#include <QtCore/QJsonDocument>
+#include <QtCore/QJsonObject>
+#include <QtNetwork/QNetworkRequest>
+#include <QtNetwork/QNetworkReply>
+
 namespace Api {
 
 Transcribes::Transcribes(not_null<ApiWrap*> api)
@@ -29,6 +38,14 @@ Transcribes::Transcribes(not_null<ApiWrap*> api)
 , _api(&api->instance()) {
 }
 
+Transcribes::~Transcribes() {
+	if (_localProcess) {
+		_localProcess->terminate();
+		_localProcess->waitForFinished();
+		delete _localProcess;
+	}
+}
+
 bool Transcribes::isRated(not_null<HistoryItem*> item) const {
 	const auto fullId = item->fullId();
 	for (const auto &[transcribeId, id] : _ids) {
@@ -96,9 +113,7 @@ int Transcribes::trialsCount() {
 }
 
 crl::time Transcribes::trialsMaxLengthMs() const {
-	return 1000 * _session->appConfig().get<int>(
-		u"transcribe_audio_trial_duration_max"_q,
-		300);
+	return 24 * 60 * 60 * 1000LL;
 }
 
 void Transcribes::toggle(not_null<HistoryItem*> item) {
@@ -108,10 +123,11 @@ void Transcribes::toggle(not_null<HistoryItem*> item) {
 		load(item);
 		_session->data().requestItemResize(item);
 	} else if (!i->second.requestId) {
-		i->second.shown = !i->second.shown;
-		if (i->second.roundview) {
-			_session->data().requestItemViewRefresh(item);
+		if (i->second.failed || i->second.result.startsWith("Downloading audio")) {
+			load(item);
+			return;
 		}
+		i->second.shown = !i->second.shown;
 		_session->data().requestItemResize(item);
 	}
 }
@@ -174,7 +190,132 @@ void Transcribes::load(not_null<HistoryItem*> item) {
 	if (!item->isHistoryEntry() || item->isLocal()) {
 		return;
 	}
-	const auto toggleRound = [](not_null<HistoryItem*> item, Entry &entry) {
+	loadLocal(item);
+}
+
+void Transcribes::loadLocal(not_null<HistoryItem*> item) {
+	const auto media = item->media();
+	const auto document = media ? media->document() : nullptr;
+	if (!document) {
+		return;
+	}
+
+	const auto id = item->fullId();
+	const auto isVideo = document->isVideoMessage();
+	const auto tempPath = QDir::tempPath()
+		+ (isVideo ? "/tg_video_" : "/tg_voice_")
+		+ QString::number(item->id.bare)
+		+ (isVideo ? ".mp4" : ".ogg");
+
+	QByteArray fileData;
+	QFile file(tempPath);
+	if (file.exists() && file.open(QIODevice::ReadOnly)) {
+		fileData = file.readAll();
+	}
+
+	if (fileData.isEmpty()) {
+		document->save(id, tempPath);
+		auto &entry = _map[id];
+		entry.result = "Downloading audio...";
+		entry.shown = true;
+		entry.pending = true;
+		entry.failed = false;
+		_session->data().requestItemResize(item);
+
+		auto timer = new QTimer();
+		timer->setInterval(500);
+		QObject::connect(timer, &QTimer::timeout, [=] {
+			if (const auto item2 = _session->data().message(id)) {
+				if (const auto doc2 = item2->media() ? item2->media()->document() : nullptr) {
+					if (!doc2->loading()) {
+						QFile check(tempPath);
+						if (check.exists() && check.size() > 0) {
+							timer->stop();
+							timer->deleteLater();
+							loadLocal(item2);
+						}
+					}
+				} else {
+					timer->stop();
+					timer->deleteLater();
+				}
+			} else {
+				timer->stop();
+				timer->deleteLater();
+			}
+		});
+		timer->start();
+		return;
+	}
+
+	QString serverPath;
+#ifdef Q_OS_WIN
+	serverPath = QCoreApplication::applicationDirPath() + "/transcribe_server.exe";
+#else
+	serverPath = QDir::homePath() + "/transcribe_server";
+#endif
+
+	auto justStarted = false;
+	if (!_localProcess) {
+		_localProcess = new QProcess();
+	}
+	if (_localProcess->state() == QProcess::NotRunning) {
+		_localProcess->start(serverPath, QStringList());
+		_localProcess->waitForStarted(3000);
+		justStarted = true;
+	}
+
+	auto &entry = _map[id];
+	entry.requestId = 0;
+	entry.shown = true;
+	entry.pending = true;
+	entry.roundview = isVideo;
+	entry.result = "Transcribing...";
+	_session->data().requestItemResize(item);
+	_session->data().requestItemViewRefresh(item);
+	_session->data().requestItemRepaint(item);
+
+	QNetworkRequest request(QUrl("http://127.0.0.1:8988/transcribe?item_id=" + QString::number(item->id.bare)));
+	request.setHeader(
+		QNetworkRequest::ContentTypeHeader,
+		isVideo ? "video/mp4" : "audio/ogg");
+	request.setTransferTimeout(120000);
+
+	const auto sendRequest = [=] {
+		const auto reply = _localNetwork.post(request, fileData);
+		QObject::connect(reply, &QNetworkReply::finished, [=] {
+			if (reply->error() == QNetworkReply::NoError) {
+				const auto data = reply->readAll();
+				const auto doc = QJsonDocument::fromJson(data);
+				const auto text = doc.object().value("text").toString();
+
+				auto &e = _map[id];
+				e.result = text;
+				e.pending = false;
+			} else {
+				auto &e = _map[id];
+				e.result = "Local transcription failed: " + reply->errorString();
+				e.pending = false;
+				e.failed = true;
+			}
+			if (const auto item = _session->data().message(id)) {
+				_session->data().requestItemResize(item);
+				_session->data().requestItemViewRefresh(item);
+				_session->data().requestItemRepaint(item);
+			}
+			reply->deleteLater();
+		});
+	};
+
+	if (justStarted) {
+		QTimer::singleShot(1500, sendRequest);
+	} else {
+		sendRequest();
+	}
+}
+
+/*
+void Transcribes::load_original(not_null<HistoryItem*> item) {
 		if (const auto media = item->media()) {
 			if (const auto document = media->document()) {
 				if (document->isVideoMessage()) {
@@ -235,6 +376,7 @@ void Transcribes::load(not_null<HistoryItem*> item) {
 	entry.failed = false;
 	entry.pending = false;
 }
+*/
 
 void Transcribes::summarize(not_null<HistoryItem*> item) {
 	if (!item->isHistoryEntry() || item->isLocal()) {
diff --git a/Telegram/SourceFiles/api/api_transcribes.h b/Telegram/SourceFiles/api/api_transcribes.h
index 7eef8a4fb6..a483b98dce 100644
--- a/Telegram/SourceFiles/api/api_transcribes.h
+++ b/Telegram/SourceFiles/api/api_transcribes.h
@@ -10,6 +10,9 @@ https://github.com/telegramdesktop/tdesktop/blob/master/LEGAL
 #include "mtproto/sender.h"
 #include "spellcheck/spellcheck_types.h"
 
+#include <QtNetwork/QNetworkAccessManager>
+#include <QtCore/QProcess>
+
 class ApiWrap;
 
 namespace Main {
@@ -30,6 +33,7 @@ struct SummaryEntry {
 class Transcribes final {
 public:
 	explicit Transcribes(not_null<ApiWrap*> api);
+	~Transcribes();
 
 	struct Entry {
 		QString result;
@@ -62,6 +66,7 @@ public:
 
 private:
 	void load(not_null<HistoryItem*> item);
+	void loadLocal(not_null<HistoryItem*> item);
 	void summarize(not_null<HistoryItem*> item);
 
 	const not_null<Main::Session*> _session;
@@ -74,8 +79,13 @@ private:
 	base::flat_map<FullMsgId, Entry> _map;
 	base::flat_map<uint64, FullMsgId> _ids;
 
+	QNetworkAccessManager _localNetwork;
+	QProcess *_localProcess = nullptr;
+
 	base::flat_map<FullMsgId, SummaryEntry> _summaries;
 
+	rpl::lifetime _lifetime;
+
 };
 
 } // namespace Api
diff --git a/Telegram/SourceFiles/data/components/sponsored_messages.cpp b/Telegram/SourceFiles/data/components/sponsored_messages.cpp
index 40c00c4bb4..d9f005df85 100644
--- a/Telegram/SourceFiles/data/components/sponsored_messages.cpp
+++ b/Telegram/SourceFiles/data/components/sponsored_messages.cpp
@@ -239,25 +239,14 @@ void SponsoredMessages::inject(
 }
 
 bool SponsoredMessages::canHaveFor(not_null<History*> history) const {
-	if (history->peer->isChannel()) {
-		return true;
-	} else if (const auto user = history->peer->asUser()) {
-		return user->isBot();
-	}
 	return false;
 }
 
 bool SponsoredMessages::canHaveFor(not_null<HistoryItem*> item) const {
-	return item->history()->peer->isBroadcast()
-		&& item->isRegular();
+	return false;
 }
 
 bool SponsoredMessages::isTopBarFor(not_null<History*> history) const {
-	if (peerIsUser(history->peer->id)) {
-		if (const auto user = history->peer->asUser()) {
-			return user->isBot();
-		}
-	}
 	return false;
 }
 
diff --git a/Telegram/SourceFiles/data/data_peer.cpp b/Telegram/SourceFiles/data/data_peer.cpp
index 3fb4d7781d..cee93d5a70 100644
--- a/Telegram/SourceFiles/data/data_peer.cpp
+++ b/Telegram/SourceFiles/data/data_peer.cpp
@@ -2003,11 +2003,6 @@ const Data::WallPaper *PeerData::wallPaper() const {
 }
 
 bool PeerData::hasActiveStories() const {
-	if (const auto user = asUser()) {
-		return user->hasActiveStories();
-	} else if (const auto channel = asChannel()) {
-		return channel->hasActiveStories();
-	}
 	return false;
 }
 
diff --git a/Telegram/SourceFiles/dialogs/dialogs_row.cpp b/Telegram/SourceFiles/dialogs/dialogs_row.cpp
index 79a1fb2d1f..9988f55d5c 100644
--- a/Telegram/SourceFiles/dialogs/dialogs_row.cpp
+++ b/Telegram/SourceFiles/dialogs/dialogs_row.cpp
@@ -441,7 +441,7 @@ void Row::PaintCornerBadgeFrame(
 	q.translate(framePadding, framePadding);
 	auto hq = std::optional<PainterHighQualityEnabler>();
 	const auto photoSize = context.st->photoSize;
-	const auto storiesCount = data->storiesCount;
+	const auto storiesCount = 0;
 	if (storiesCount) {
 		hq.emplace(q);
 		const auto line = st::dialogsStoriesFull.lineTwice / 2.;
diff --git a/Telegram/SourceFiles/dialogs/dialogs_widget.cpp b/Telegram/SourceFiles/dialogs/dialogs_widget.cpp
index ac7b80c848..072a59fa3c 100644
--- a/Telegram/SourceFiles/dialogs/dialogs_widget.cpp
+++ b/Telegram/SourceFiles/dialogs/dialogs_widget.cpp
@@ -420,12 +420,7 @@ Widget::Widget(
 	object_ptr<Ui::IconButton>(this, st::dialogsLock))
 , _scroll(this)
 , _scrollToTop(_scroll, st::dialogsToUp)
-, _stories((_layout != Layout::Child)
-	? std::make_unique<Stories::List>(
-		this,
-		st::dialogsStoriesList,
-		_storiesContents.events() | rpl::flatten_latest())
-	: nullptr)
+, _stories(nullptr)
 , _searchTimer([=] { search(); })
 , _peerSearch(&controller->session(), Api::PeerSearch::Type::WithSponsored)
 , _singleMessageSearch(&controller->session()) {
@@ -782,7 +777,7 @@ Widget::Widget(
 	}
 
 	setupFrozenAccountBar();
-	setupTopBarSuggestions();
+	// setupTopBarSuggestions();
 #ifdef _DEBUG
 	setupTopBarSuggestionTestHotkeys();
 #endif // _DEBUG
diff --git a/Telegram/SourceFiles/history/view/history_view_transcribe_button.cpp b/Telegram/SourceFiles/history/view/history_view_transcribe_button.cpp
index d414422435..1d02688f34 100644
--- a/Telegram/SourceFiles/history/view/history_view_transcribe_button.cpp
+++ b/Telegram/SourceFiles/history/view/history_view_transcribe_button.cpp
@@ -330,22 +330,7 @@ void TranscribeButton::paint(
 }
 
 bool TranscribeButton::hasLock() const {
-	const auto session = &_item->history()->session();
-	if (session->premium()) {
-		return false;
-	}
-	const auto transcribes = &session->api().transcribes();
-	if (_summarize) {
-		return transcribes->summary(_item).premiumRequired;
-	}
-	if (transcribes->freeFor(_item) || transcribes->trialsCount()) {
-		return false;
-	}
-	const auto until = transcribes->trialsRefreshAt();
-	if (!until || base::unixtime::now() >= until) {
-		return false;
-	}
-	return true;
+	return false;
 }
 
 void TranscribeButton::setOpened(bool opened, Fn<void()> update) {
@@ -396,19 +381,7 @@ ClickHandlerPtr TranscribeButton::link() {
 				}
 			}
 		} else {
-			const auto max = session->api().transcribes().trialsMaxLengthMs();
-			const auto doc = _item->media()
-				? _item->media()->document()
-				: nullptr;
-			if (doc && (doc->isVoiceMessage() || doc->isVideoMessage())) {
-				if (doc->duration() > max) {
-					if (const auto controller = my.sessionWindow.get()) {
-						controller->uiShow()->showToast(
-							tr::lng_audio_transcribe_long(tr::now));
-						return;
-					}
-				}
-			}
+
 			if (summarize) {
 				session->api().transcribes().toggleSummary(item);
 			} else {
diff --git a/Telegram/SourceFiles/history/view/media/history_view_document.cpp b/Telegram/SourceFiles/history/view/media/history_view_document.cpp
index 51b786de86..026b2848e7 100644
--- a/Telegram/SourceFiles/history/view/media/history_view_document.cpp
+++ b/Telegram/SourceFiles/history/view/media/history_view_document.cpp
@@ -371,7 +371,7 @@ Document::Document(
 	if (isRound) {
 		const auto &entry = _data->session().api().transcribes().entry(
 			realParent);
-		_transcribedRound = entry.shown;
+		_transcribedRound = entry.shown || isRound;
 	}
 
 	createComponents();
@@ -446,7 +446,7 @@ bool Document::dataLoaded() const {
 
 void Document::createComponents() {
 	uint64 mask = 0;
-	if (_data->isVoiceMessage() || _transcribedRound) {
+	if (_data->isVoiceMessage() || _data->isVideoMessage() || _transcribedRound) {
 		mask |= HistoryDocumentVoice::Bit();
 	} else {
 		mask |= HistoryDocumentNamed::Bit();
@@ -477,7 +477,7 @@ void Document::createComponents() {
 		voice->seekl = (!media || !media->ttlSeconds())
 			? std::make_shared<VoiceSeekClickHandler>(_data, [](FullMsgId) {})
 			: nullptr;
-		if (_transcribedRound) {
+		if (_transcribedRound || _data->isVideoMessage()) {
 			voice->round = std::make_unique<::Media::Player::RoundPainter>(
 				_realParent);
 		}
@@ -500,12 +500,7 @@ QSize Document::countOptimalSize() {
 		const auto media = _parent->data()->media();
 		if ((media && media->ttlSeconds())
 			|| _realParent->isScheduled()
-			|| _realParent->isAdminLogEntry()
-			|| (!session->premium()
-				&& !transcribes->freeFor(_realParent)
-				&& !transcribes->trialsSupport())
-			|| (!session->premium()
-				&& _data->duration() > transcribes->trialsMaxLengthMs())) {
+			|| _realParent->isAdminLogEntry()) {
 			voice->transcribe = nullptr;
 			voice->transcribeText = {};
 		} else {
@@ -591,7 +586,7 @@ QSize Document::countOptimalSize() {
 	if (thumbed) {
 		accumulate_max(maxWidth, tleft + MaxStatusWidth(_data) + tright);
 	} else {
-		auto unread = (_data->isVoiceMessage() || _transcribedRound)
+		auto unread = (_data->isVoiceMessage() || _data->isVideoMessage() || _transcribedRound)
 			? (st::mediaUnreadSkip + st::mediaUnreadSize)
 			: 0;
 		accumulate_max(maxWidth, tleft + MaxStatusWidth(_data) + unread + _parent->skipBlockWidth() + st::msgPadding.right());
@@ -829,7 +824,7 @@ void Document::draw(
 				inner,
 				context.selected());
 		if (!coverDrawn) {
-			if (_transcribedRound) {
+			if (_transcribedRound || _data->isVideoMessage()) {
 				if (const auto voice = Get<HistoryDocumentVoice>()) {
 					if (const auto &round = voice->round) {
 						if (round->fillFrame(inner.size())) {
diff --git a/Telegram/SourceFiles/history/view/media/history_view_gif.cpp b/Telegram/SourceFiles/history/view/media/history_view_gif.cpp
index ebb84988a6..e4a18f15f7 100644
--- a/Telegram/SourceFiles/history/view/media/history_view_gif.cpp
+++ b/Telegram/SourceFiles/history/view/media/history_view_gif.cpp
@@ -1535,6 +1535,7 @@ TextState Gif::textState(QPoint point, StateRequest request) const {
 			return state;
 		}
 	}
+
 	if (QRect(usex + paintx, painty, usew, painth).contains(point)) {
 		ensureDataMediaCreated();
 		if (_spoiler && !_spoiler->revealed) {
@@ -2616,9 +2617,7 @@ void Gif::ensureTranscribeButton() const {
 	if (_data->isVideoMessage()
 		&& (!media || !media->ttlSeconds())
 		&& !_parent->data()->isScheduled()
-		&& !_parent->data()->isAdminLogEntry()
-		&& (_data->session().premium()
-			|| _data->session().api().transcribes().trialsSupport())) {
+		&& !_parent->data()->isAdminLogEntry()) {
 		if (!_transcribe) {
 			_transcribe = std::make_unique<TranscribeButton>(
 				_realParent,
diff --git a/Telegram/SourceFiles/history/view/media/history_view_media_common.cpp b/Telegram/SourceFiles/history/view/media/history_view_media_common.cpp
index b46a9aa0a7..b134f60074 100644
--- a/Telegram/SourceFiles/history/view/media/history_view_media_common.cpp
+++ b/Telegram/SourceFiles/history/view/media/history_view_media_common.cpp
@@ -156,6 +156,8 @@ std::unique_ptr<Media> CreateAttach(
 					parent,
 					document,
 					skipPremiumEffect));
+		} else if (document->isVideoMessage()) {
+			return std::make_unique<Document>(parent, parent->data(), document);
 		} else if (document->isAnimation() || document->isVideoFile()) {
 			return std::make_unique<Gif>(
 				parent,
diff --git a/Telegram/SourceFiles/info/profile/info_profile_widget.cpp b/Telegram/SourceFiles/info/profile/info_profile_widget.cpp
index 6f7a339bd3..31020ade11 100644
--- a/Telegram/SourceFiles/info/profile/info_profile_widget.cpp
+++ b/Telegram/SourceFiles/info/profile/info_profile_widget.cpp
@@ -332,10 +332,6 @@ rpl::producer<QString> Widget::title() {
 }
 
 rpl::producer<Dialogs::Stories::Content> Widget::titleStories() {
-	const auto peer = controller()->key().peer();
-	if (peer && !peer->isChat()) {
-		return Dialogs::Stories::LastForPeer(peer);
-	}
 	return nullptr;
 }
 
